%p#notice= notice
#wrapper
  .taskText{autocomplete: "off", contenteditable: "true"}
    = render 'nextTaskText'
  %p.mistakeCounter
    %span.mistakeCount
    left
  #correct{style: "display:none"}
    %h2= @task.encourag
    = link_to("Next →", @task.next_task, remote: true) if @task.next_task
  = javascript_tag do
    window.referencetext = "#{j @task.reference}";