$('.taskTextHightlighted').fadeOut(animTm)
$('.taskAuthor').animate({opacity: '0'}, animTm)
$('.endContainer').fadeOut(animTm)
window.referencetext = '<%= escape_javascript(@task.reference) %>'

setTimeout(function(){
	$('.nextBtn').replaceWith('<%= escape_javascript(render "nextBtn")%>') // replace href for next link
	$('.taskText').html('<%= escape_javascript(render "nextTaskText")%>').css('margin-top','260px').attr('contenteditable', 'true').animate({opacity: '1'}, animTm) //update task text
	$('.taskTextHightlighted').html('<%= escape_javascript(render "nextTaskTextHighlighted")%>') //update error explonations for new task
	$('.ribbonCurrentNumber').html('<%= escape_javascript(render "layouts/currentTaskNumber") %>') //Change current task number
	outputMistakes() //update mistaks counter
	$('.mistakeCounter').fadeIn(animTm) //show mistake counter

  initMistakesExplonation($('mistake:first')) //make first error highlighted
}, animTl)
	

